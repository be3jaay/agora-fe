import { CheckoutScreen } from "@/components/xstream/checkout-screen";

export const metadata = {
  title: "Checkout — SukiCloser",
  description: "Voice-driven AI checkout — Nova will guide you through your order.",
};

export default function CheckoutPage() {
  return <CheckoutScreen />;
}
